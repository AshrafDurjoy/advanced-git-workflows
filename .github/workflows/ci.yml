name: CI pipeline
on: [push]
jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v2
            - name: run a command
              run: echo " Hello, World!"